import { describe, it } from 'vitest'
import { AxiosClient } from '@/lib/HttpClient.ts'

describe('HttpClient', () => {
  const httpClient = new AxiosClient()

  it('should install http client', () => {})
})
